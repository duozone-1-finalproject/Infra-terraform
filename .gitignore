# Local .terraform directories
.terraform/

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Ignore override files as they are usually local-only
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore variable files if they contain sensitive data
*.tfvars
*.tfvars.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Lock file (optional: 팀 내에서 provider 버전을 고정하려면 포함해도 됨)
.terraform.lock.hcl
.terraform/
.terraform.lock.hcl
